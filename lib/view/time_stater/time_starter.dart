import 'package:flutter/material.dart';

class TimeStarter extends StatelessWidget {
  const TimeStarter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}